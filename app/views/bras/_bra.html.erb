<div class="bra clearfix">
  <img src="<%= bra.image_url %>">
  <p>Brand: <%= bra.brand %></p>
  <p>Model: <%= bra.model %></p>
  <p>This is a <%= bra.style %> bra.</p>
  <div class="actions">
    <% if @index %>
      <%= link_to "Details and sizes", bra %>
    <% end %>
    <%= link_to "Edit", edit_bra_path(bra) %>
    <%= link_to "Delete", bra_path(bra), method: 'delete' %>
  </div>
</div>
