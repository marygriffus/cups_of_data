<%= form_for @bra do |f| %>

  <%= f.label :brand %>
  <%= f.text_field :brand %>

  <%= f.label :model %>
  <%= f.text_field :model %>

  <%= f.label :image_url%>
  <%= f.text_field :image_url %>

  <%= f.submit %>

<% end %>
